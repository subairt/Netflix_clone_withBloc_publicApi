import 'package:flutter/material.dart';
import 'package:netflix_clone/core/colors/colors.dart';

const kwidth = SizedBox(width: 10);
const khight = SizedBox(height: 10);
const khight15 = SizedBox(height: 15);
const khight20 = SizedBox(
  height: 20,
);

// Radius

final kRadius10 = BorderRadius.circular(10);
final kRadius30 = BorderRadius.circular(30);

// home banner 3 text syle

const style =
    TextStyle(color: Colors.white, fontSize: 16, fontWeight: FontWeight.bold);
const styleMnth = TextStyle(color: kwhiteColor, fontSize: 20);
const styleNumber = TextStyle(
    color: kwhiteColor,
    letterSpacing: 4,
    fontSize: 28,
    fontWeight: FontWeight.bold);

const imageAppendUrl = 'https://image.tmdb.org/t/p/w500';
